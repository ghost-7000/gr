# ⚠️ FIX: حساب الأدمن لا يعمل؟

الخطأ `Invalid login credentials` يعني: **المستخدم غير موجود** أو **كلمة المرور غير صحيحة**.

بما أنك شغلت السكريبت النظري، فإنه يهيئ الجداول فقط، ولكنه **لا يستطيع إنشاء حساب مستخدم حقيقي** (لأسباب أمنية في Supabase تمنع إنشاء باسوورد عبر SQL).

## الحل البسيط (خطوتين فقط):

1. **أنشئ الحساب يدوياً:**
   - افتح: `http://localhost:3000/register`
   - الاسم: `Admin`
   - الإيميل: `admin@gmail.com`
   - كلمة المرور: `123456`
   - اضغط **Register**.

2. **رقِّه إلى أدمن:**
   - ارجع لـ **Supabase SQL Editor**.
   - شغل هذا الأمر البسيط:
     ```sql
     UPDATE public.profiles SET role = 'admin' WHERE email = 'admin@gmail.com';
     UPDATE auth.users SET raw_user_meta_data = raw_user_meta_data || '{"role": "admin"}'::jsonb WHERE email = 'admin@gmail.com';
     ```

الآن ارجع لصفحة `http://localhost:3000/login` وسجل الدخول، سيعمل 100% ويفتح لوحة التحكم. 🚀
