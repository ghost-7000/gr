# 🚀 الخطوات النهائية لإطلاق مشروع GRMC

مبروك! لقد تم الانتهاء من جميع التعديلات البرمجية على الموقع ليكون مطابقاً للمشروع القديم (dow2) ولكن بتقنيات حديثة (Next.js + Supabase).

لاستخدام الموقع بشكل كامل، يجب عليك تنفيذ الخطوات البسيطة التالية بدقة:

---

## 1️⃣ إعداد قاعدة البيانات (خطوة "الخلاص" ☢️)
لإصلاح جميع مشاكل تسجيل الدخول، الأدمن، والبيانات، قمنا بإنشاء سكريبت شامل يحذف القديم ويبني الجديد بشكل صحيح.

1. اذهب إلى **[Supabase Dashboard](https://supabase.com/dashboard)**.
2. ادخل على مشروعك -> **SQL Editor**.
3. انقر على **New query**.
4. افتح الملف `scripts/final_setup.sql` الموجود في مشروعك، انسخ محتواه بالكامل، والصقه في المحرر.
   - **ملاحظة:** هذا السكريبت سيحذف جميع البيانات القديمة ويبدأ بصفحة نظيفة.
5. اضغط **RUN**.

---

## 2️⃣ إعداد تخزين الصور (Storage) 🖼️
لتفعيل رفع صور المنتجات وبلاغات "تواصل معنا"، يجب إعداد الـ Bucket يدوياً:

1. في Supabase Dashboard، اذهب إلى قائمة **Storage** (من القائمة الجانبية).
2. انقر على **Create new bucket**.
3. سمِّ الـ Bucket باسم: `images` (أحرف صغيرة فقط).
4. **مهم جداً:** فعّل خيار **Public bucket** (لتظهر الصور للناس).
5. اضغط **Save**.
6. (اختياري) يمكنك رفع صور المنتجات يدوياً داخل مجلد `products` في هذا الـ Bucket إذا أردت تغيير الصور الافتراضية.

---

## 3️⃣ إنشاء حساب الأدمن 👑
بعد تشغيل السكريبت في الخطوة 1، لن يكون هناك أي مستخدمين. لإنشاء أدمن:

1. اذهب للموقع: `http://localhost:3000/register`
2. سجل حساب جديد باسم: `Admin` وإيميل: `admin@gmail.com` (أو أي إيميل تفضله).
3. بعد التسجيل، سيوجهك للصفحة الرئيسية كمستخدم عادي.
4. لترقيته إلى أدمن:
   - ارجع لـ **Supabase SQL Editor**.
   - شغل هذا الأمر البسيط (غيّر الإيميل إذا سجلت بغيره):
     ```sql
     UPDATE public.profiles SET role = 'admin' WHERE email = 'admin@gmail.com';
     UPDATE auth.users SET raw_user_meta_data = raw_user_meta_data || '{"role": "admin"}'::jsonb WHERE email = 'admin@gmail.com';
     ```
5. سجل خروج ودخول مجدداً، وستظهر لك لوحة التحكم.

---

## ✅ المميزات الجديدة:
- **نظام الفواتير:** بعد الدفع، ستظهر فاتورة قابلة للطباعة (`/invoice`) مطابقة للقديم.
- **تواصل معنا:** يمكنك الآن رفع صور للبلاغات.
- **إدارة المخزون:** الشراء يخصم من المخزون تلقائياً.
- **السلة والمفضلة:** تعمل بشكل ممتاز وتفرغ عند الطلب.

استمتع بمشروعك الجديد! 🚀
